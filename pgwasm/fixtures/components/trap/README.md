# `trap` fixture

Exports `boom: func()` which traps immediately (used for `HV00` / external routine exception coverage).

Built with **`wasm32-unknown-unknown`**.

## Rebuild

```bash
cd "$(dirname "$0")/.." && ./build_all.sh
```
